# TODO: get user from cognito via query resolver using userPoolId & username
type User {
  id: ID!
  email: AWSEmail
  phone: AWSPhone
  firstName: string!
  lastName: string!
  birthday: AWSDate!
}

# TODO: authorize customers group to create students
# TODO: create student mutation resolver using StudentProps
type Student @model {
  id: ID!
  user: User!
}

input StudentProps {
  email: AWSEmail!
  phone: AWSPhone!
  firstName: string!
  lastName: string!
  birthday: AWSDate!
}
